


function infiniteLoop(noover)
{
    for ( i = 0;  ; )
    printf(noover)
}function emy_mulp_param(param)
{
    param = [];
    
    while(index < param.length)
    {
        param[index]*2;
    } 
}